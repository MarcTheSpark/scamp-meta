# Python / build artifacts
__pycache__/
*.pyc
*.egg-info/
build/
dist/
dist-*/

# Local environments & IDE
.venv/
.idea/
.claude/settings.local.json

# Workspace-local, not shared
scratch/
.mcp.json

# Deploy target with host details — copy deploy.local.sh.example (see uploadDocs.sh)
deploy.local.sh

# Built docs — deployed via uploadDocs.sh, never committed
docs_build/

# Generated docs artifacts (produced by the docs build; regenerated, not committed)
docs/build_text
docs/make.bat
docs/Makefile
docs/examples
docs/scamp.rst
docs/clockblocks.rst
docs/expenvelope.rst
docs/pymusicxml.rst
docs/scamp
docs/clockblocks
docs/expenvelope
docs/pymusicxml
docs/scamp_extensions.*

# Rendered example media (audio/scores) — regenerated by render_example_media.py.
# A few hard-to-auto-render ones (OSC, octave lines) are committed anyway.
docs/_static/media
